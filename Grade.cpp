#include<iostream>
using namespace std;
int main()
{
int m, bio, phy,chem, comp, total,per;
cout<<"marks in maths, marks in biology, marks in physics, marks in chemistry, marks in computer";
per=(total*100)/500;
if (per>=90)
{cout<<"grade A";}
else
if (per>=80)
{cout<<"grade B";}
if (per>=70)
{cout<<"grade C";}
if (per>=60)
{cout<<"grade D";}
if (per>=40)
{cout<<"grade E";}
if (per<40)
{cout<<"grade F";}
return 0;
}

